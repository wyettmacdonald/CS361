class ParserTestAbramsDeutschDurstJones {
  var y = 4;
  var b = code.elegant(10);
  var[] array = new Array();
  int main(int z) {
    var x = new Walrus();
    var a = 10 instanceof bob;
    var c = cast (int, 10);

    while (x <= 5)
      x = x + y;
    
    for(i = 0; i < 10; i ++)
      break;

    if (true || false && true)
      return 10;
    if (i <= 8)
      break;
    else
      return 9;
  }
}
